---
title: Cost dashboard
description: Per-persona compile cost across the marketplace LLM eval gate. Auto-generated.
---

> **Auto-generated.** This page is rewritten by `scripts/marketplace-cost-dashboard.ts`
> on every `marketplace-eval-llm.yml` run. Hand edits will be clobbered — touch the
> generator instead.

The marketplace LLM eval gate compiles the top-N personas through real Gemini every
night and records token + cost telemetry per persona. This page summarises the trailing
window so cost regressions are visible at a glance.

## No data yet

`eval-reports/` is empty. The first nightly run of `marketplace-eval-llm.yml`
will populate this dashboard on the next 04:30 UTC tick.

_Generated at `2026-05-05T08:04:18.098Z`._
