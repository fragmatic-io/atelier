{
  "$schema": "../persona-fixture.schema.json",
  "name": "customer-context-panel",
  "route": "/customers/context",
  "title": "Customer context panel — risk-aware customer drilldown",
  "intent": {
    "user_id": "persona-customer-context",
    "profile_version": 1,
    "updated_at": "2026-05-03T00:00:00.000Z",
    "global_preferences": {
      "density": "comfortable",
      "color_mode": "light",
      "motion_preference": "subtle",
      "automation_trust": "cautious",
      "modal_tolerance": "low"
    },
    "lenses": { "ops": "customer_context_panel" },
    "rules": [
      {
        "scope": "*",
        "rule": "Always show the Risk pane before Timeline; risk drives action.",
        "version": 1,
        "locked": true
      },
      {
        "scope": "ops",
        "rule": "Render customers as a list of 6 rows with a 3-tab right pane (Overview, Risk, Timeline).",
        "version": 1
      },
      {
        "scope": "ops",
        "rule": "Bind the customer list to `customer.list` with no `filter` or `sort` — the capability already returns the active accounts in the right order. If you must filter, prefer a CEL-like string (`\"health == 'at_risk'\"`) over a structured filter object.",
        "version": 1
      }
    ],
    "vocabulary": {
      "customer": "the account currently in focus",
      "risk_note": "free-form ops note attached to a customer health signal"
    }
  },
  "capabilities": {
    "customer.list": {
      "id": "customer.list",
      "kind": "data",
      "version": "1.0.0",
      "input": {},
      "output": {
        "id": "string",
        "name": "string",
        "segment": "string",
        "health": "string",
        "plan": "string",
        "arr": "string",
        "owner": "string"
      },
      "side_effects": [],
      "permissions": ["customer:read"],
      "confirmation": "none",
      "reversible": true
    },
    "customer.timeline": {
      "id": "customer.timeline",
      "kind": "data",
      "version": "1.0.0",
      "input": { "customer_id": "string" },
      "output": { "ts": "string", "kind": "string", "title": "string" },
      "side_effects": [],
      "permissions": ["customer:read"],
      "confirmation": "none",
      "reversible": true
    },
    "customer.risk_notes.open": {
      "id": "customer.risk_notes.open",
      "kind": "action",
      "version": "1.0.0",
      "input": { "customer_id": "string" },
      "output": { "opened_at": "string" },
      "side_effects": ["audit"],
      "permissions": ["customer:read"],
      "confirmation": "none",
      "reversible": true
    }
  },
  "components": [
    "Container",
    "Stack",
    "Split",
    "List",
    "DetailView",
    "Tabs",
    "Timeline",
    "Drawer",
    "Button",
    "Alert",
    "EmptyState",
    "Skeleton"
  ],
  "layout_constraints": {
    "shape": "two-pane (left: customer list, right: tabbed customer detail)",
    "list_rows_visible": 6,
    "tabs": ["Overview", "Risk", "Timeline"],
    "show_risk_notes_drawer_trigger": true,
    "primary_action": "customer.risk_notes.open"
  },
  "brand_kit_id": "atelier.design.neutral",
  "few_shot_summary": "CustomerContextPanel renders a 1280x720 surface with a customer List on the left (6 rows) and a tabbed DetailView on the right with Overview / Risk / Timeline tabs, plus a Drawer-trigger Button to open risk notes."
}
