// SPDX-License-Identifier: MIT
/**
 * {{appName}} — root component.
 *
 * The Vite scaffold is intentionally lean: a static page that points at
 * the next steps. Once you've authored a recipe / capability set, swap
 * this body for `<CirRoute>` from `@atelier/react` and wire the runtime.
 */
export function App(): React.JSX.Element {
  return (
    <main style={{ padding: 24, maxWidth: 720, margin: '0 auto', fontFamily: 'system-ui, sans-serif' }}>
      <h1>{{appName}}</h1>
      <p>
        Welcome to your new Atelier app, scaffolded with Vite + React 19.
      </p>
      <ul>
        <li>
          Capabilities live in <code>capabilities/</code>
        </li>
        <li>
          Skills live in <code>skills/</code>
        </li>
        <li>
          Recipes live in <code>recipes/</code>
        </li>
        <li>
          Policies live in <code>policies/</code>
        </li>
        <li>
          Brand tokens live in <code>brand-kit.json</code>
        </li>
      </ul>
      <p>
        Next: edit <code>src/atelier-server.ts</code> to wire the
        compile pipeline, then drop <code>{'<CirRoute>'}</code> from
        <code> @atelier/react</code> in here.
      </p>
    </main>
  );
}
