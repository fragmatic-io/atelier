// SPDX-License-Identifier: MIT
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

/**
 * {{appName}} — Vite + React 19 starter.
 *
 * Atelier's runtime + react packages are ESM. Vite handles them natively.
 * The compile pipeline (capabilities, manifest cache) lives in
 * `src/atelier-server.ts` and is exposed via the dev middleware below.
 */
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    // The Vite dev server proxies the manifest API to our middleware
    // by default; no special config needed.
  },
  build: {
    target: 'es2022',
    sourcemap: true,
  },
});
