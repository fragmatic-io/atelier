// SPDX-License-Identifier: MIT
/** @type {import('next').NextConfig} */
const nextConfig = {
  // {{appName}} — generated by `atelier init` (Sprint 1.1).
  // Atelier's runtime bundles ESM-only deps; Next 15 handles them natively.
  // No special webpack overrides required for the standalone scaffold.
};

export default nextConfig;
