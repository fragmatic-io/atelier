// SPDX-License-Identifier: MIT
export default function Page() {
  return (
    <main style={{ padding: 24, maxWidth: 720, margin: '0 auto' }}>
      <h1>{{appName}}</h1>
      <p>
        Welcome to your new Atelier app. The manifest endpoint at{' '}
        <code>/api/cir/manifest/[user_id]/[app_id]/[route]</code> wires up
        the compiler, manifest cache, and audit sink.
      </p>
      <ul>
        <li>
          Capabilities live in <code>capabilities/</code>
        </li>
        <li>
          Skills live in <code>skills/</code>
        </li>
        <li>
          Recipes live in <code>recipes/</code>
        </li>
        <li>
          Policies live in <code>policies/</code>
        </li>
        <li>
          Brand tokens live in <code>brand-kit.json</code>
        </li>
      </ul>
      <p>
        Edit <code>app/page.tsx</code> to swap in <code>{'<CirRoute>'}</code> from{' '}
        <code>@atelier/react</code> and start composing.
      </p>
    </main>
  );
}
