{
  "name": "@cir/compiler",
  "version": "0.0.0",
  "private": true,
  "description": "CIR compiler service — turns capabilities + skills + components + intent + trigger into a manifest",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "dependencies": {
    "@google/genai": "^1.50.1",
    "redis": "^5.12.1"
  }
}
