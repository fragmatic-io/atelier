{
  "name": "@cir/data-resolvers",
  "version": "0.1.0",
  "private": true,
  "description": "CIR data resolver adapters — REST, OpenAPI, GraphQL, Mock, Composite, plus a SWR-style cache wrapper. Plug into the `DataResolver` protocol from @cir/react so manifest data bindings resolve to real data without hand-rolling a fetch layer.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "vitest run"
  },
  "dependencies": {
    "@cir/schemas": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
