{
  "name": "@cir/capability-resolver",
  "version": "0.0.0",
  "private": true,
  "description": "CIR capability scoping — substring + two-stage (tiny-model) resolvers that plug into ToolUsingCompiler's SemanticSearch seam",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "dependencies": {
    "@cir/compiler": "workspace:*",
    "@cir/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
