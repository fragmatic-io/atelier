{
  "name": "@atelier/recipe-resolver",
  "version": "0.0.0",
  "private": true,
  "description": "Atelier recipe retrieval — substring + embedding resolvers + LocalRecipeStore that plug into the C-5 findRecipe compiler tool",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "dependencies": {
    "@atelier/schemas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
