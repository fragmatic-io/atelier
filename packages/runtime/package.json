{
  "name": "@atelier/runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Atelier render runtime — framework-agnostic core: manifest fetcher/resolver, action dispatcher, trigger bus, component registry, render-plan derivation",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "vitest run"
  },
  "dependencies": {
    "@atelier/policies": "workspace:*",
    "@atelier/schemas": "workspace:*",
    "idb-keyval": "^6.2.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "publishConfig": {
    "access": "restricted"
  }
}
