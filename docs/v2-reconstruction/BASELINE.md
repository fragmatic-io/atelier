# V2 reconstruction baseline

This branch reconstructs missing V2.2 work; it does not claim to recover an unavailable release.

## Inputs verified on 2026-09-06

- Repository base: `5ead443e942cea1b840da1541369bcfded0f385a`.
- Surviving archive: `atelier-v2.1-audited.zip`.
- Archive SHA-256: `b1959c718c2a62260a2d1b307336c8b7e24ac1a6e4096c606aa7a1ffb636ef7c`.
- Archive package version: `2.1.0-rc.1`.
- No V2.2 source archive or commit was available. Prior prose, screenshots and test-count claims are requirements/evidence leads, not source recovery.

## Reconstruction contract

Each promised feature is mapped to implementation paths, executable acceptance checks, and an explicit status. Original passing tests must be rerun, not copied as a new result. Source-generation breadth, reviewed publication, conversational artifacts, API inventory, host-session execution, product voice and project isolation must receive additional tests. Live proprietary-provider calls and arbitrary-host certification must not be claimed without execution.

This branch preserves `main`; no force push, repository deletion or production deployment is authorized by this work. Public commits must exclude credentials, operational databases, private customer snapshots and private signing keys.
